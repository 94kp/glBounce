Vmath.h is the file that requires "cpp" file, since C programs do not accept headers with C++ code.
vmath is used from the red book.
vmath.h contains the maths from glu in this file.

When to use <, > and when to use "" (double quotes)

